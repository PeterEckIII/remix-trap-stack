describe("User model", () => {
  test("should create a user", async ({ integration }) => {
    console.log(`Integration function: ${integration.createNormalUser}`);
    expect(true).toBe(true);
  });
});
